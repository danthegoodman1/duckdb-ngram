# name: test/sql/ngram.test
# description: ngram extension loads and exposes trigrams()
# group: [sql]

# Before we load the extension, this will fail
statement error
SELECT trigrams('duck');
----
Catalog Error

# Require statement will ensure this test is run with this extension loaded
require ngram

query I
SELECT trigrams('duck');
----
[duc, uck]
